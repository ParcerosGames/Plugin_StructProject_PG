**Roadmap:**

- Update OS View Editor Hierarchy Custom
- Update the way to instantiate the objectID
- Update the information in the Readme (Pass the information to a separate readme)
- Update the component aggregate, other than by name set, to be by name in OS Editor Hierarchy Custom
- Separate the code from AddComponentToEmpty
- Separate the code from instantiating the objectID
